/**
 * Drizzle schema — the single source of truth for the database shape.
 * Migrations in `drizzle/` are generated from this file and CI fails if the two
 * drift apart (M0.2). Tables land per milestone, starting with M1.
 */
export {};
